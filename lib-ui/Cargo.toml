[package]
name = "lib-ui"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { version = "0.18.*", default-features = false, features = ["dynamic_linking", "2d"] }

[dependencies.lib-core]
path = "../lib-core"

[dependencies.lib-inventory]
path = "../lib-inventory"
