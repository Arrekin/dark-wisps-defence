[package]
name = "lib-grid"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { version = "0.19.*", default-features = false, features = ["dynamic_linking", "2d"] }

[dependencies.lib-core]
path = "../lib-core"
